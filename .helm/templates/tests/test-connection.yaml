apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "concerto.fullname" . }}-test-connection"
  labels:
    {{- include "concerto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "concerto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

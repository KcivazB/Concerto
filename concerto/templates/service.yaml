apiVersion: v1
kind: Service
metadata:
  name: {{ include "concerto.fullname" . }}
  labels:
    {{- include "concerto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "concerto.selectorLabels" . | nindent 4 }}
